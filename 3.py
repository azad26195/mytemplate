for _ in xrange(input()):
	n=int(raw_input())
	b=map(int,raw_input().split())
	di=[]
	vi=[]
	c=0
	for i in b:
		if(i<0):
			di.append(b.index(i))
		else:
			vi.append(b.index(i))
	# print di,vi
	di=sorted(di,reverse=True)
	vi=sorted(vi,reverse=True)
	#print di,vi
	p=len(di)
	q=len(vi)
	while(p):
		while(q)
			if (x-y)<0:
				c+=b[y]*abs(x-y)
				q-=1
				
			else 
